resources:
  containers:
  - container: app
    image: marcusriemer/sqlino:test
  - container: postgres
    image: postgres:10.0
    env:
      POSTGRES_DB: 'esqulino_test'
      POSTGRES_USER: 'esqulino'


pool:
  vmImage: 'ubuntu-16.04'

container: app

services:
  db: postgres

steps:
- script: make -C client install-deps
# - script: make -C client client-compile universal-compile
# - script: make -C client cli-compile
- script: make -C client client-test