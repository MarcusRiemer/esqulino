mutation UpdateNews(
    $id: ID!,
    $title: LangJson!,
    $text: LangJson!,
    $publishedFrom: ISO8601DateTime!
) {
    updateNews(input:{
        id:$id,
        title:$title,
        text:$text,
        publishedFrom:$publishedFrom
    }) {
        news {
            id
            createdAt
            updatedAt
            publishedFrom
            text
            title
            userId
            __typename
        }
        errors
        __typename
    }
}