mutation ProjectAddUsedBlockLanguage(
  $projectId: ID!,
  $blockLanguageId: ID!
){
  addUsedBlockLanguage(
    input: {
      projectId: $projectId,
      blockLanguageId: $blockLanguageId
    }
  ) {
    result {
      blockLanguage {
        id
        name
      }
    }
  }
}