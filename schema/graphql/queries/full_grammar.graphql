query FullGrammar($id: ID!) {
  grammar(id: $id) {
    id
    name
    programmingLanguageId
    slug
    generatedFromId
    types
    foreignTypes
    visualisations
    foreignVisualisations
    root
    includes
    visualizes
    blockLanguages {
      id
      name
    }
  }
}
