mutation DestroyAssignmentRequiredCodeResource($id: ID!) {
  destroyAssignmentRequiredCodeResource(input: { id: $id }) {
    project {
      id
      assignments {
        id
        assignmentRequiredCodeResources {
          id
        }
      }
    }
  }
}
