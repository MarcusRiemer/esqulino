mutation AcceptInvitation($projectId: ID!) {
  acceptInvitation(input: { projectId: $projectId }) {
    project {
      id
      projectMembers {
        id
        membershipType
        joinedAt
        user {
          id
          displayName
        }
      }
    }
  }
}
