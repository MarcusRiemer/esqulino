mutation CreateProgrammingLanguageProject(
  $projectName: String!,
  $languageName: String!
  $description: String!,
) {
  createProgrammingLanguageProject(input: {
    projectName: $projectName,
    languageName: $languageName,
    description: $description
  }) {
    id
  }
}
