mutation DestroyProjectCourseParticipation($groupId: ID!) {
  destroyProjectCourseParticipation(input: { groupId: $groupId }) {
    project {
      id
      participantProjects {
        id
      }
    }
  }
}
