mutation CreateProject(
  $name: LangJson!
  $slug: String
  $courseTemplate: Boolean
) {
  createProject(
    input: { name: $name, slug: $slug, courseTemplate: $courseTemplate }
  ) {
    errors
    id
  }
}
