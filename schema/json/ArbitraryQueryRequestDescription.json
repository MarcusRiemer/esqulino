{
  "$ref": "#/definitions/ArbitraryQueryRequestDescription",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "definitions": {
    "ArbitraryQueryRequestDescription": {
      "additionalProperties": false,
      "description": "Some servers support execution of arbitrary queries. This is intended\nto be used during development and can be done via this request-type.",
      "properties": {
        "params": {
          "$ref": "#/definitions/QueryParamsDescription"
        },
        "sql": {
          "type": "string"
        }
      },
      "required": [
        "params",
        "sql"
      ],
      "type": "object"
    },
    "QueryParamsDescription": {
      "additionalProperties": {
        "type": "string"
      },
      "description": "Parameters are simply a key-value dictionary. Whenever a query\nmakes use of user-bound parameters, these are transferred via\nthis kind of object.",
      "type": "object"
    }
  }
}