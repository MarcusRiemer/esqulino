{
  "$ref": "#/definitions/ProjectUpdateDescription",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ApiVersionToken": {
      "description": "It would be nicer if versions could be numbers, but Typescript has no\nconcept of union types for numbers.",
      "enum": [
        "1",
        "2",
        "3",
        "4"
      ],
      "type": "string"
    },
    "ProjectName": {
      "description": "The name of the project. May only contain more or less friendly\ncharacters.",
      "pattern": "^[a-zA-Z0-9 \\-_\\?äöüÄÖÜß:]{4,}$",
      "type": "string"
    },
    "ProjectUpdateDescription": {
      "additionalProperties": false,
      "description": "These things can be provided when updating the project itself.",
      "properties": {
        "activeDatabase": {
          "type": "string"
        },
        "apiVersion": {
          "$ref": "#/definitions/ApiVersionToken",
          "description": "These API versions are merely a number, no need to overcomplicate things.\nIf versions differ, any implementing the API is free to specify\nversion ranges it works with."
        },
        "description": {
          "type": "string"
        },
        "indexPageId": {
          "type": "string"
        },
        "name": {
          "$ref": "#/definitions/ProjectName"
        },
        "preview": {
          "type": "string"
        },
        "projectUsesBlockLanguages": {
          "items": {
            "$ref": "#/definitions/ProjectUpdateUsedBlockLanguage"
          },
          "type": "array"
        }
      },
      "required": [
        "apiVersion"
      ],
      "type": "object"
    },
    "ProjectUpdateUsedBlockLanguage": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "blockLanguageId": {
              "type": "string"
            }
          },
          "required": [
            "blockLanguageId"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "_destroy": {
              "type": "boolean"
            },
            "id": {
              "type": "string"
            }
          },
          "required": [
            "id",
            "_destroy"
          ],
          "type": "object"
        }
      ]
    }
  }
}