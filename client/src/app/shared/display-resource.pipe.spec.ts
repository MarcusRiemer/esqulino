import { DisplayResourcePipe } from "./display-resource.pipe";

describe("DisplayResourcePipe", () => {
  it("create an instance", () => {
    //const pipe = new DisplayResourcePipe();
    //expect(pipe).toBeTruthy();
  });
});
