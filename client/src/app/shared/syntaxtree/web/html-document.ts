import { NodeDescription } from '../syntaxtree.description';

import { FrontMatterDescription } from './html-document.description';

export class HtmlDocument {
  constructur(desc: NodeDescription) {

  }
}

export class FrontMatter {
  constructur(desc: FrontMatterDescription) {
  }
}