query AdminListProjects($first:Int,$after:String,$before:String,$last:Int,$public:Boolean) {
    projects(first:$first,after:$after,before:$before,last:$last,public:$public) {
        nodes{
            id
            name {
                de
                en
            }
            slug
            codeResources{
                totalCount
            }
        }
        totalCount
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
    }
}