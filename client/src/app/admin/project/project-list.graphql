query AdminListProjects($first:Int,$after:String,$before:String,$last:Int,$order:ProjectOrder) {
    projects(first:$first,after:$after,before:$before,last:$last,order:$order) {
        nodes{
            id
            name {
                de
                en
            }
            slug
            codeResources{
                totalCount
            }
        }
        totalCount
        pageInfo {
            hasPreviousPage
            hasNextPage
            startCursor
            endCursor
        }
    }
}