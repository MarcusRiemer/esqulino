mutation UpdateNews(
    $id: ID!,
    $title: LangJson!,
    $text: LangJson!,
    $publishedFrom: ISO8601DateTime
) {
    updateNews(input:{
        id:$id,
        title:$title,
        text:$text,
        publishedFrom:$publishedFrom
    }) {
        id
        errors
    }
}